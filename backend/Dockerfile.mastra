FROM node:22-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .

# Copy .env if provided in build context
COPY --chown=node:node .env* ./

EXPOSE 4111
CMD ["npx", "mastra", "dev", "--dir", "src/mastra"]
